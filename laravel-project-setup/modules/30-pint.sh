# name: pint
# desc: Write pint.json (psr12 + custom rules)
# default: on
#
# Style rules live in assets/pint.json — edit that file to change the house
# style; this module only puts it in place.

install_asset pint.json pint.json
